# Great Expectations Stub

This directory will house data quality suites and checkpoints as we build out curation in later milestones. For milestone 1 we only scaffold the structure; no validations are executed yet.

Suggested layout for future work:
- `expectations/` for suite YAML/json
- `checkpoints/` for Prefect-triggered checkpoints
- `uncommitted/` for GE runtime artifacts (excluded from git)

